static int var(int x, int y) { return x % y; }

// * La palabra clave STATIC  nos sirve para decirle al compilador que tal
// variable
// * solo sera leida dentro de un mismo documento,
// < esto quiere decir que si la queremeos vicular a otro archivo no se podra
// < solo podra ser utilizada dentro de un mismo archivo
